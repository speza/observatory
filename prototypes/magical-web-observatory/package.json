{
  "name": "ao-magical-web-observatory-poc",
  "version": "0.0.0-disposable",
  "private": true,
  "description": "Disposable Mineral Atlas refinement for the web Observatory; not production code.",
  "type": "module",
  "scripts": {
    "start": "vite --host 127.0.0.1 --port 4173",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "latest",
    "typescript": "latest",
    "vite": "latest"
  }
}
